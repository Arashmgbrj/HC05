راهنمای کامل پروژه ماژول بلوتوث HC-05 با آردوینو
📋 فهرست مطالب
معرفی ماژول

پیش‌نیازها

نحوه اتصال

پیکربندی AT Commands

کد نمونه

عیب‌یابی

کاربردها

1️⃣ معرفی ماژول HC-05
ماژول HC-05 یک ماژول بلوتوث کلاس 2 سریال (SPP) است که ارتباط بی‌سیم نقطه‌به‌نقطه یا نقطه‌به‌چند نقطه را فراهم می‌کند. این ماژول معمولاً برای ارتباط بین آردوینو و دستگاه‌هایی مثل تلفن هوشمند، لپ‌تاپ یا سایر ماژول‌های بلوتوث استفاده می‌شود.

مشخصات فنی:

ولتاژ کاری: 3.6 تا 6 ولت

پروتکل: بلوتوث 2.0 + EDR

محدوده: 10 متر (در فضای باز)

حالت‌های کاری: Master، Slave، Master-Slave

پین‌های اصلی: VCC, GND, TXD, RXD, EN, STATE

2️⃣ پیش‌نیازها
قطعات مورد نیاز:
برد آردوینو (Uno, Nano, Mega, etc.)

ماژول بلوتوث HC-05

مقاومت‌های 1kΩ و 2kΩ (برای تقسیم ولتاژ)

برد بورد و سیم جامپر

نرم‌افزارها:
نرم‌افزار آردوینو IDE

اپلیکیشن بلوتوث ترمینال روی موبایل (مثل Serial Bluetooth Terminal)

3️⃣ نحوه اتصال
نکته مهم: ولتاژ منطقی ماژول HC-05 3.3V است، اما پین RX آردوینو ولتاژ 5V دارد. برای محافظت از ماژول، از تقسیم ولتاژ استفاده کنید:

روش اتصال ایمن:

ماژول HC-05  <-->  آردوینو Uno
--------------------------------
      VCC     <-->      5V
      GND     <-->      GND
      TXD     <-->      RX (پین 0)
      RXD     <-->      تقسیم ولتاژ به TX (پین 1)
      EN      <-->      (متصل نشود یا به 3.3V)


      مدار تقسیم ولتاژ برای پین RX ماژول:


پین TX آردوینو (5V) → مقاومت 2kΩ → پین RX ماژول → مقاومت 1kΩ → GND


4️⃣ پیکربندی AT Commands
برای تغییر تنظیمات ماژول (مانند نام، پین، سرعت باد) از دستورات AT استفاده می‌شود:

مراحل پیکربندی:
اتصال در حالت AT Mode:

پین KEY یا EN ماژول را قبل از روشن کردن به 3.3V وصل کنید

LED ماژول با سرعت کم (هر 2 ثانیه) چشمک می‌زند

برنامه آردوینو برای AT Mode:
void setup() {
  Serial.begin(38400);  // سرعت پیش‌فرض HC-05 در حالت AT
  Serial.println("AT Command Mode");
}

void loop() {
  if(Serial.available()) {
    char c = Serial.read();
    Serial.write(c);  // اکو کاراکتر
  }
}

AT                 // تست ارتباط
AT+NAME?           // مشاهده نام ماژول
AT+NAME=NewName    // تغییر نام ماژول
AT+PSWD?           // مشاهده پین
AT+PSWD="1234"     // تغییر پین به 1234
AT+UART?           // مشاهده تنظیمات سریال
AT+UART=9600,0,0   // تغییر سرعت به 9600 بیت بر ثانیه
AT+VERSION?        // مشاهده نسخه فریم‌ور
AT+RESET           // ریست ماژول


5️⃣ کد نمونه
مثال 1: ارتباط ساده سریال
// ارتباط ساده بین آردوینو و موبایل از طریق بلوتوث
// آردوینو داده دریافتی از بلوتوث را به Serial Monitor می‌فرستد
// و داده دریافتی از Serial Monitor را به بلوتوث ارسال می‌کند

void setup() {
  Serial.begin(9600);       // ارتباط با کامپیوتر
  Serial1.begin(9600);      // ارتباط با HC-05 (در آردوینو Mega)
  // برای آردوینو Uno از SoftwareSerial استفاده کنید
}

void loop() {
  // اگر از بلوتوث داده دریافت شد، در Serial Monitor نمایش بده
  if(Serial1.available()) {
    char received = Serial1.read();
    Serial.print("Received: ");
    Serial.println(received);
  }
  
  // اگر از Serial Monitor داده دریافت شد، به بلوتوث بفرست
  if(Serial.available()) {
    char toSend = Serial.read();
    Serial1.write(toSend);
  }
}


مثال 2: کنترل LED از طریق بلوتوث


#include <SoftwareSerial.h>

// تعریف پین‌های SoftwareSerial برای آردوینو Uno
SoftwareSerial bluetooth(2, 3); // RX, TX

const int ledPin = 13;
char command;

void setup() {
  pinMode(ledPin, OUTPUT);
  digitalWrite(ledPin, LOW);
  
  Serial.begin(9600);
  bluetooth.begin(9600);
  Serial.println("Bluetooth LED Controller Ready");
}

void loop() {
  if(bluetooth.available()) {
    command = bluetooth.read();
    Serial.print("Command: ");
    Serial.println(command);
    
    switch(command) {
      case '1':
        digitalWrite(ledPin, HIGH);
        bluetooth.println("LED ON");
        break;
      case '0':
        digitalWrite(ledPin, LOW);
        bluetooth.println("LED OFF");
        break;
      default:
        bluetooth.println("Invalid command. Send 1 or 0");
    }
  }
}


